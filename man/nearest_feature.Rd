% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/supportive_function.R
\name{nearest_feature}
\alias{nearest_feature}
\title{Find nearest spatial feature}
\usage{
nearest_feature(x, y)
}
\arguments{
\item{x}{An \code{sf} or \code{sfc} object representing origin features.}

\item{y}{An \code{sf} or \code{sfc} object representing target features.}
}
\value{
An \code{sf} object identical to \code{x}, with two additional columns:
\describe{
\item{y_index}{Index of the nearest feature in \code{y} for each element of \code{x}.}
\item{distance}{Distance from each feature in \code{x} to its nearest \code{y}.}
}
}
\description{
For each feature in \code{x}, finds the nearest feature in \code{y}
and computes the distance to it.
}
\examples{
\dontrun{
library(sf)
x <- st_as_sf(data.frame(lon = 1:3, lat = 1:3), coords = c("lon", "lat"), crs = 4326)
y <- st_as_sf(data.frame(lon = c(2, 4), lat = c(2, 4)), coords = c("lon", "lat"), crs = 4326)
nearest_feature(x, y)
}

}
