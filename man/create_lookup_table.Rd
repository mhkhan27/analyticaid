% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xml_to_label.R
\name{create_lookup_table}
\alias{create_lookup_table}
\title{Create a lookup table mapping XML names to human labels}
\usage{
create_lookup_table(survey, choices, label = "label::English (en)")
}
\arguments{
\item{survey}{A data frame of the XLSForm \strong{survey} sheet; must include
columns \code{type}, \code{name}, and the label column specified in \code{label}.}

\item{choices}{A data frame of the XLSForm \strong{choices} sheet; must include
columns \code{list_name}, \code{name}, and the label column specified in \code{label}.}

\item{label}{Character scalar giving the exact label column name to use
(e.g., \code{"label::English (en)"}).}
}
\value{
A data frame with two columns:
\describe{
\item{col_name_lable}{Target (unique) label to rename to.}
\item{xml_label}{Source XML column name to rename from.}
}
}
\description{
Builds a mapping from the XLSForm \code{survey}/\code{choices} sheets to support
renaming data columns from XML \code{name} to readable labels. Expands
\code{select_multiple} questions into \code{name.choice} style entries and includes
parent \code{select_multiple} columns.
}
\examples{
\dontrun{
lut <- create_lookup_table(survey, choices, label = "label::English (en)")
head(lut)
}
}
