% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xml_to_label.R
\name{xml_to_label_only_concat}
\alias{xml_to_label_only_concat}
\title{Convert select-multiple values to concatenated labels (non-destructive)}
\usage{
xml_to_label_only_concat(data, survey, choice, label = "label::English")
}
\arguments{
\item{data}{A data frame containing XML-named columns.}

\item{survey}{XLSForm \code{survey} sheet.}

\item{choice}{XLSForm \code{choices} sheet.}

\item{label}{Label column to use (default \code{"label::English"}).}
}
\value{
The input \code{data} with select-multiple columns converted to
comma-separated label strings (or \code{NA} when no matches).
}
\description{
For each select-multiple question present in \code{data}, converts encoded
response values into a comma-separated list of human-readable choice labels.
Keeps original column names and replaces values in place.
}
\examples{
\dontrun{
df_sm <- xml_to_label_only_concat(df, survey, choice, "label::English")
}
}
