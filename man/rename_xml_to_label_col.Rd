% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xml_to_label.R
\name{rename_xml_to_label_col}
\alias{rename_xml_to_label_col}
\title{Rename data frame columns from XML names to labels}
\usage{
rename_xml_to_label_col(data, survey, choices, label)
}
\arguments{
\item{data}{A data frame whose columns (XML names) you want to rename.}

\item{survey}{XLSForm \code{survey} data frame (see \code{create_lookup_table()}).}

\item{choices}{XLSForm \code{choices} data frame (see \code{create_lookup_table()}).}

\item{label}{Character scalar naming the label column to use in both sheets.}
}
\value{
A data frame with columns renamed where matches exist.
}
\description{
Uses \code{create_lookup_table()} to build a mapping, then renames columns of
\code{data} where XML \code{name} matches, replacing with human-readable labels.
Only existing columns are affected.
}
\examples{
\dontrun{
df2 <- rename_xml_to_label_col(df, survey, choices, "label::English (en)")
}
}
